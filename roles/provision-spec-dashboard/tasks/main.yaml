- name: download the latest kubernetes dashboard yaml configuration
  get_url:
    url: https://raw.githubusercontent.com/kubernetes/dashboard/master/src/deploy/recommended/kubernetes-dashboard.yaml
    dest: "{{ config.directory }}/k8s/specs/kubernetes-dashboard.yaml"

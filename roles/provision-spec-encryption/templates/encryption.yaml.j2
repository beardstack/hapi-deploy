kind: EncryptionConfig
apiVersion: v1
resources:
- resources:
  - secrets
  providers:
  - aescbc:
      keys:
      - name: key1
        secret: "{{ kubernetes.encryptionKey | b64encode }}"
  - identity: {}

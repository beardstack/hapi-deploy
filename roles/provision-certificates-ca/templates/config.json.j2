{
  "signing": {
    "default": {
      "expiry": "{{ certificates.expiration }}"
    },
    "profiles": {
      "kubernetes": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "{{ certificates.expiration }}"
      }
    }
  }
}
